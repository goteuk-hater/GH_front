import {StyleSheet, View} from 'react-native';
import StyledText from '../../components/globalcomponents/StyledText';

export const QAScreen = () => {
  return (
    <View>
      <StyledText>QAScreen</StyledText>
    </View>
  );
};
const styles = StyleSheet.create({});
